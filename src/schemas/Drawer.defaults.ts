import { DrawerSchema } from "./Drawer.schema"

export const DrawerDefaults: DrawerSchema = {
  drawerId: "",
  catalogId: "",
  cards: [],
  numberOfCards: 1,
  levelName: "BEGINNER",
  frequency: "DAILY",
}
